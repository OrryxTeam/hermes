# Core runtime dependencies.
# Tested with Python 3.12, jax 0.10, flax 0.12 (NNX), optax 0.2,
# orbax-checkpoint 0.12, astropy 8.
absl-py
jax
jaxlib
flax
optax
orbax-checkpoint
ml_collections
numpy
scipy
astropy  # only needed for the `fits` light-curve source

# Development (tests): pip install pytest chex
